import { LoginForm } from "../components";

export default function Login(){
    return (
        <main className="mt-24 min-h-screen">
            <section>
                <LoginForm />
            </section>
        </main>
    )
}